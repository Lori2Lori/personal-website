$("button").on("click", function() {
  $('p').addClass("well")
})

// on() is a function that takes 2 parameters:
// 1. first is an event name
// 2. second is a function
// 'p' means paragraphs in html
